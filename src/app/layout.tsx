import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Toaster } from '@/components/ui/toaster'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'ECL Webapp - IFRS 9 Expected Credit Loss Calculator',
  description: 'Professional ECL calculation platform for financial institutions',
  keywords: ['IFRS 9', 'ECL', 'Expected Credit Loss', 'Financial Risk', 'Banking'],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Toaster />
      </body>
    </html>
  )
}